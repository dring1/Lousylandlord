<% if comment.valid? %> 
	<%= content_tag :p, comment.comment, class:'comment-text ' %> 
		<% if signed_in? %>
        	<%= link_to 'Delete', comment, method: :delete, data: { confirm: "You sure?" } %>
	<% end %>	
<% end %>